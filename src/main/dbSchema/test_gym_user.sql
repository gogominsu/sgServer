/*CREATE TABLE test_gym_user(
                              user_number BIGINT AUTO_INCREMENT NOT NULL,
                              user_id VARCHAR(64) UNIQUE NOT NULL,
                              user_password VARCHAR(255) NOT NULL,
                              user_name VARCHAR(64) NOT NULL,
                              member_since DATE DEFAULT SYSDATE()smartgymsmartgymsmartgym,
                              user_phone INT,
                              user_email VARCHAR(127) NOT NULL, PRIMARY KEY(user_number)
);*/

